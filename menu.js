import promptSync from 'prompt-sync';
const prompt = promptSync(); 

1.  const menus = [];

   while (i=1, i<10, i++) {
    if (i===menu1)  {
    let menu1 = alert('Hi friend!');
    menus.push(menu1)
    console.log(alert());

    } else if (i===2, i===menu2) {
2.  let menu2 = alert('joke of the week! Why do programmers prefer dark mode? Because the light attracts bugs!');
    menus.push(menu2);
    console.log(alert());
    
    } else if (i===3, i===menu3) {
3.  let menu3
    const now= new Date();
    const currentTime = now.toLocaleTimeString();
    console.log(`${currentTime}`)
    menus.push(menu3)
    
    } else if (i===4, i===menu4) {
4.  let menu4 = prompt()
    prompt('pick a word: bat') 
    alert('now reverse the word!')

    menus.push(menu4)
    
    } else if (i===5, i===menu5) {
    
5.  let menu5 = number;
    let number = 1;
    while (number <= 10) {
        if (number % 2 === 0) {
            console.log(`${number} is even!`)
        } else {
            console.log(`${number} is odd!`)
        }
        number++
    }
    menus.push(menu5)
    
    } else if (i===6, i===menu6) {
6.  let menu6 = prompt('key in any number between 1 and 10!')
    let number1 = 1;
    while (number1 <= 10) {
        let num2 = console.log(number1**2)
        alert(`${num2}`)
    menus.push(menu6)
    }
    
    } else if (i===7, i===menu7) {
7.  const phrase = prompt('key in a phrase that you resonate with:')
    const repetitionCount = parseInt(prompt("Please enter the number of times to repeat the phrase:"));
    if (isNaN(repetitionCount) || repetitionCount < 0) {
      console.log("Invalid repetition count. Please enter a non-negative number.");
       } else {
  // Repeat the phrase the specified number of times
   const repeatedPhrase = phrase.repeat(repetitionCount);

  // Display the repeated phrase
      console.log(repeatedPhrase);
       }
    menus.push(menu7)
    
    } else if (i===8, i===menu8) {
     const temp = prompt('Key in any celsius value!');
8.  //ask for a celsius value and convert it to farenheit using the formula:
    F=C * 1.8 + 32
    menus.push(menu8)
    
    } else if (i===9, i===menu9) {
9.  //prompt for a number and count down to zero 
    const num2 = prompt('key in a number');
    if ( num2>=0, num2-=)
        console.log(num2)
    menus.push(menu9)
    
    } else {
10. 
    alert('cheers! Have a good one!')  
    console.log(alert) 
    menus.push(menu10)
    }
}